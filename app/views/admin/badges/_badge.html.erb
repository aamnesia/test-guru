<tr class="text-center">
  <td><%= image_tag("#{badge.image_url}", size: "128x128") %></td>
  <td><%= badge.title %></td>
  <td><%= badge.rule %> <%= badge.parameter %></td>
  <td><%= link_to 'Show', admin_badge_path(badge), class: 'btn btn-dark'%></td>
  <td><%= link_to 'Edit', edit_admin_badge_path(badge), class: 'btn btn-dark'%></td>
  <td><%= button_to 'Delete',
                admin_badge_path(badge),
                method: :delete,
                class: 'btn btn-dark'%></td>
<tr>
