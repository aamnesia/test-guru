<tr class="text-center">
  <td class="text-left"><%= question.body %></td>
  <td><%= link_to 'Show', admin_question_path(question), class: 'btn btn-light'%></td>
  <td><%= link_to 'Edit', edit_admin_question_path(question), class: 'btn btn-light'%></td>
  <td><%= button_to 'Delete',
                admin_question_path(question),
                method: :delete,
                class: 'btn btn-light'%></td>
</tr>
